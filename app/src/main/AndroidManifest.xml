<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".SummerWeatherApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".ui.Demo.ContentProviderActivity"
            android:exported="false" />

        <service
            android:name=".ui.Demo.MyService"
            android:enabled="true"
            android:exported="true" />
        <!--
        <provider
            android:authorities=""
            android:name=".ui.Demo.MyContentProvider"
            android:exported="true">
        </provider>
        -->
        <activity
            android:name=".ui.Demo.DemoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.book.BookListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.weather.WeatherActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>